redis:
    image: redis
    container_name: cache
    expose:
        - 6379

app:
    build: ./
    volumes:
        - ./:/var/www/app
    links:
        - redis
    ports:
        - ${LISTEN_PORT}:${LISTEN_PORT}
    environment:
        - NODE_ENV=development
    command: yarn start

